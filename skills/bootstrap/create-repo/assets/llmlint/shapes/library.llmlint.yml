# Library / package / service judge-level rules (plugin). Pairs with shapes/library.md.
version: 1

rules:
  - name: stable_public_surface
    description: |
      when the target files mark what is public versus internal (explicit exports,
      `__all__`, or visibility modifiers) so the public API is intentional, rather
      than exposing internal implementation with no public/internal distinction.
